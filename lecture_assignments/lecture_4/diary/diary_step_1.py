# Skapa en class som heter Diary

# Classen ska ha attributen:
    # "entries" - En lista av entries
    # "name" - En sträng med dagbokens namn
    # "owner" - En sträng med dagbokens ägare

# Classen ska ha metoderna: 
    # "add_entry" - Lägger till ett dagboksinlägg 
    # "get_entries" - Returnerar alla entries
    # "get_entry" - Returnerar ett entry baserat på ett index

# När du skapat classen, gör detta under classen:
    # 1. Skapa en lista av 2 stycken entries
    # 2. Skapa en instans av classen Diary, skicka in din lista av 2 stycken entries
    # 3. Lägg till ett extra entry genom att använda metoden "add_entry".
    # 4. Spara ner det som metoden "get_entries" returnerar i en variabel och skriv ut den.
    # 5. Ge metoden "get_entry" ett index och spara ner det som returneras i en variabel och skriv ut den.


    
